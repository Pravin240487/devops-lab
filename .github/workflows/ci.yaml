name: CI
on:
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Show git status
      run: |
        git --version
        echo "Running basic checks"
    - name: Lint
      run: |
        # pretend lint step
        echo "lint ok"

